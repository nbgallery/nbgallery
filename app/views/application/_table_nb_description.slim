-if nb.description.length > 500
  div.nb-description.comment.more
    span.sr-only Description of Notebook:
    p.minimize
      ==render_description(nb.description, true)
    a.view-full.tooltip-big title="#{strip_tags(nb.description).sub("\"","&quot;")}" tabindex="0" Read Full
-else
  div.nb-description.comment.more
    span.sr-only Description of Notebook:
    p
      ==render_description(nb.description)
