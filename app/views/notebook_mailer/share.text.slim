' Notebook Shared with you
= "\r\n" * 2
' #{@owner.name}
- if !@owner.org.blank?
  '  (#{@owner.org})
' has shared with you the notebook #{@notebook.title}: #{@url + notebook_path(@notebook)}

-unless @message.blank? || @email_needs_to_be_simplified
  = "\r\n" * 2
  ' message: #{@message}
